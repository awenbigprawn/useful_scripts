# Ubuntu 常用快捷键备忘

## Codex

- `Ctrl+U`：快速删除当前输入。

## VS Code（Markdown）

- `Ctrl+K`，然后按 `V`：在侧边打开预览（Open Preview to the Side）。
- `Ctrl+Shift+V`：在当前编辑器窗口打开预览。

## Chrome

- `Ctrl+Tab`：切换到右侧的下一个标签页。
- `Ctrl+Shift+Tab`：切换到左侧的上一个标签页。
- `Ctrl+1`～`Ctrl+8`：切换到对应编号的标签页。
- `Ctrl+9`：切换到最后一个标签页。
